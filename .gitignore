# Local ebook examples / reference files (not part of the repo)
ejemplos_ebooks/

# Figma Make bundle (Vite app under figma_make/)
figma_make/node_modules/

# Claude Code: git worktree copies (large, duplicate node_modules; not the canonical tree)
.claude/worktrees/

# Supabase CLI local state
supabase/.temp/
supabase/.branches/
supabase/functions/.env

# Local secrets for email template push script (see supabase/email-templates/README.md)
supabase/email-templates/.env.local
