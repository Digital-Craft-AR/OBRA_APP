# Copy to .env.local (gitignored) — never commit real tokens.

# Supabase Dashboard → Account → Access Tokens (personal access token, prefix sbp_)
SUPABASE_ACCESS_TOKEN=

# Project Settings → General → Reference ID (not the anon key)
SUPABASE_PROJECT_REF=
